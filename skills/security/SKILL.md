# Skill: Security

Review secrets, storage, logging, network handling, validation, dependencies, and release configuration.

Never output secrets discovered in the repository.
Redact sensitive values in explanations.
