# Skill: Release

Perform a pre-release verification without deploying.

Check analyzer, tests, generated files, versioning, environment configuration, secrets, permissions, logging, and platform configuration.

Report blockers separately from recommendations.
