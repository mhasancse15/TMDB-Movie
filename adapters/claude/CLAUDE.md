# Flutter AI Agent — Claude Code Adapter

Use the global `flutter-ai-agent` skill for Flutter/Dart repositories.

Read `SKILL.md` and relevant files under `rules/`, `commands/`, and `skills/`.

Always:
- inspect existing project patterns
- adapt to current architecture
- avoid unrelated refactors
- run format/analyze/tests after meaningful changes

Project-local `CLAUDE.md` instructions take precedence where they are more specific.
