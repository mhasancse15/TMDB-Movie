# Flutter AI Agent — GitHub Copilot Adapter

Use the global `flutter-ai-agent` skill when working on Flutter/Dart.

First inspect:
- pubspec.yaml
- project instructions
- lib/
- test/

Adapt to existing architecture and state management.

Do not introduce unrelated dependencies or architecture migrations.

For meaningful changes:
- format
- analyze
- run relevant tests
- synchronize generated code when applicable
