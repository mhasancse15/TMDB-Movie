# Provider Rules

Follow the project's established Provider/ChangeNotifier pattern.

- Keep mutable state encapsulated.
- Dispose resources owned by ChangeNotifier.
- Avoid exposing unnecessary mutable collections.
- Keep API/database calls outside UI widgets.
