# Git Rules

- Keep changes focused.
- Do not overwrite unrelated user changes.
- Inspect `git diff` before committing.
- Never reset/delete work without explicit authorization.
- Use descriptive commit messages.
- Do not commit generated artifacts unless the project convention requires them.
