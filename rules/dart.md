# Dart Rules

- Use sound null safety.
- Prefer immutable values.
- Use `final` by default.
- Use descriptive names.
- Keep functions focused.
- Avoid unnecessary dynamic types.
- Prefer exhaustive pattern matching where it improves clarity.
- Document public APIs when the project convention requires it.
- Do not introduce advanced language features without a project need.
