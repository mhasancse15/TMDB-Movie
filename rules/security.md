# Security Rules

Never commit secrets.

Do not expose:
- API keys
- access tokens
- passwords
- private keys
- production credentials

Avoid sensitive logging.

Validate untrusted input.

Use secure storage where the project's security model requires it.
