# Example Project Instructions

This file belongs in the root of an actual Flutter project.

## Project-specific stack

Replace these values with the real project:
- State management:
- Architecture:
- Router:
- Networking:
- Persistence:

## Project rules

- Follow existing code patterns.
- Prefer the project's current dependencies.
- Run `dart format .`, `flutter analyze`, and relevant tests.
- Do not commit secrets.

For generic Flutter workflows, use the global `flutter-ai-agent` skill.
